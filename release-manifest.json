{
  "schema_version": 1,
  "version": "0.28.2",
  "channel": "stable",
  "auto_update": true,
  "requires_manual_approval": false,
  "database_revision": "0025_alert_recovery_resolution",
  "minimum_current_version": "0.5.3",
  "notes": "Automatically resolve an automation failure alert only when its exact job run later succeeds, preserve the original failure evidence, expose the live linked run state, and backfill stale recovered alerts with audit records."
}
