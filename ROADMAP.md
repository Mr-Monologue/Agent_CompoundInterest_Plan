# 当前交付：v0.38.0 持仓论点治理阶段结项（2026-09-24）

已发布、安装并完成实际只读验收：论点内容确认、版本历史、证据复核、日常组合摘要及复盘；历史买入理由保持未知，研究不改变现行金额。双平台CI、备份、迁移保全及安装一致性通过，具体论点仍待用户确认。完整完成标准与限制以 docs/LAUNCH_CHECKLIST.md 最终节为准。

本阶段持续部署授权已终止。C/D最小方案和E条件见 docs/SHADOW_MODEL_MINIMUM_PLAN.md；模型未激活。独立数据/披露差异、账户材料及其他具体映射仍未完成；通知收件、休眠/重启和长期场景未经本轮验证。自动获取持仓与交易后置。

以下为历史记录，不覆盖上述状态。

# 当前阶段：持仓论点治理与日常复盘闭环（2026-09-24）

用户已明确授权本阶段连续开发、测试、发布、备份、部署与现场验收。v0.38.0实现研究内容确认、版本化生效、证据复核与日常摘要，生产安装/验收尚待最终补记。范围与原文以 docs/LAUNCH_CHECKLIST.md 文末为准；具体论点和映射仍待内容确认，不改变现行金额或策略。

C/D最小方案及E衔接见 docs/SHADOW_MODEL_MINIMUM_PLAN.md；模型未启动或激活。通知、休眠/重启及自动获取持仓交易边界不变。

以下为历史记录，不覆盖上述状态。

# 当前交付：v0.37.3 阶段结项（2026-09-24）

恢复逻辑已区分代码兼容与数据库快照权限，保留当前事实，未知兼容阻断；完整、部分结束、跳过及响应丢失防重已完成隔离HTTP验收。双平台发布、安装一致性和实际只读验收通过。当前四项限定工程阶段已结束，持续部署授权随之终止。

完整结项、研究/账户数据与真实场景缺口、v1.7 A—E剩余及下一阶段验收范围见 docs/STAGE_CLOSEOUT.md；唯一证据索引见 docs/LAUNCH_CHECKLIST.md。真实试发、休眠/重启、整夜观察和生产真实投资闭环未通过；下一阶段尚未授权。

以下为历史记录，不覆盖上述状态。

# 历史交付：v0.37.2 恢复保护与阶段授权（2026-09-24）

已完成双平台发布与实际安装验收；阶段授权及四项完成标准见 docs/LAUNCH_CHECKLIST.md 当前授权节。回滚先验证旧代码与当前库兼容，禁止自动覆盖当前数据库；纯审计记录变化也会保守阻断，自动恢复闭环仍有明确限制。当前阶段尚未结束，外部依赖与具体投资确认保持独立。

以下为历史记录，不覆盖上述状态。

# 当前源码发布：v0.37.1 研究摘要兼容修复

持久化小数显示与公布路径标识修复已完成长期分支、双平台 CI 及发布树核验。个人安装和现场结果只读本机私有交接；下列为历史记录。

# 当前交付：v0.37.0 组合研究复核（已发布，待本机升级确认）

- 本机基线 v0.36.0；本轮组合摘要、公布基准路径及通知证据状态已完成源码发布和隔离验收。正式发布/部署结果以 docs/LAUNCH_CHECKLIST.md 文末为准。
- 不增加数据库迁移；策略、财务事实及正式风险配置不在本轮变更范围。其余基金候选未确认、数据缺失和真实运行场景继续独立列明。

以下为历史交付记录：

# 当前交付：v0.36.0 基准诊断（2026-09-24，已上线）

- 已实现独立版本化复合研究映射、历史分段复利、净值/分红比较及披露差异警告。本机已升级至 v0.36.0，0040 迁移、八项研究入口与十个真实窗口验收通过；原业务不变，映射待确认。完整证据见唯一清单最终补记。
- 入口见 [BENCHMARK_RESEARCH](docs/BENCHMARK_RESEARCH.md)；既有账户、通知、模型和稳定性待办保留。

以下为已有交付记录：

# 当前入口与交付状态（2026-09-23）

- v0.35.0 已发布并于 2026-09-23 在本机完成实际 HTTP 验收：版本化研究草稿、收益来源画像、MAPER、决策日志、只读风险覆盖提案与独立额度观察。0039 迁移、七项已安装入口和原业务不变性通过；研究草稿不等于论点激活。账户取证独立待办，优先 040046，其他基金只补影响执行的缺失项。

- v0.34.0 已发布并在本机上线验收：可复用证据、受治理账户渠道约束、分段日历拆单与升级恢复修复。0038 迁移、五项真实 HTTP 入口、业务不变性及安装文件核验通过；账户执行约束仍待证据和确认，详见唯一清单。

- 已发布并在本机验收：v0.33.0 HTTP 日常入口；Windows 调度沿用 v0.32.0。
- 已发布并本机验收 v0.33.1：执行约束未知提示、Core 研究诊断及候选架构接手资料；不激活 v1.7。
- 项目背景：[PROJECT_BACKGROUND](docs/PROJECT_BACKGROUND.md)。
- 唯一执行进度与交接：[LAUNCH_CHECKLIST](docs/LAUNCH_CHECKLIST.md)。
- 日常操作：[操作说明](docs/CODEX_DAILY_OPERATIONS.md)。
- 通知投递、开机未登录、真实休眠与长期稳定性仍未验收。

---

以下为历史阶段记录，版本状态和职责边界仅适用于记录当时；不能作为当前生产状态。

## v0.32.0 独立后台调度（开发审查阶段，2026-09-17）

- 当前生产只读核验：v0.31.8，HTTP health/ready PASS；本轮没有生产升级或调度切换。
- 新增 Core 调度来源确认与原子领取、Windows 独立 HTTP worker、本地失败日志、24 小时补跑及工作台心跳。
- 首批仅每日行情、每日风险、系统健康；政策原值复用，Hermes 通知投递与其他任务未迁移。
- 部署脚本默认预览、安装保持禁用。历史三次漏跑不自动补跑。参见 [部署与回滚预案](docs/windows-background-scheduler.md)。
- 已通过本地 Windows 全套 288 项及最终相关 35 项测试、真实隔离 HTTP 进程实测、Ruff、mypy 和 PowerShell 语法检查。Ubuntu/Windows CI 结果随审查 PR 核对；不将测试等同生产运行通过。

# Roadmap

## Windows 本机 Codex 优先

- v0.31.8 待发布增补：本机 MCP 接入、只读连通性预检、客户端身份与无 Hermes 安装升级。
- 先在一个 Codex 入口完成查询、计划预览及复盘，不以多 Agent 兼容作为当前验收目标。
- 后续单独迁移 Hermes Cron 和通知，保留去重、漏跑恢复和明确的生产变更授权。

## v0.31.8 — 零投入周正式记录

- 用户明确整周不投入时，直接记录受治理的零投入决定，不先生成或冻结虚假的资产分配计划。
- 保存准确的七日起止日期、周预算、零执行、全额放弃、不结转、原因和说明。
- 创建及提交时复核重叠计划、真实买入和在途申购；过期未确认计划草稿不视为计划事实。
- 提交后形成 `SKIPPED` 终态并复用正式周报流程，不创建任何财务事实。
- 提供预览、草稿、查询、续签和提交能力，并同步正式 Hermes 操作技能。

## v0.31.7 — 正式周度复盘与报告闭环

- 周报绑定确定的周计划，并使用计划持久化的起止日期。
- 支持预览、确认草稿、正式提交、不可覆盖版本和有原因的重新生成。
- 完整保留计划、场外申购、确认、毛额交易、手续费和 DATE_ONLY 时间精度事实。
- 估值证据不足时只降级估值部分，不阻断交易事实周报，也不替换历史净值。
- 自动补报与人工补报共用幂等正式存储，默认不发送微信。

## v0.31.6 — 部分执行周计划正式收尾

- 增加 `PARTIALLY_EXECUTED_CLOSED`，保留计划、执行、未执行和逐基金历史事实。
- 通过可续签的收尾草稿和用户明确确认完成原子状态转换，不产生财务事实。
- 未执行余额明确放弃且不结转，从后续计划阻塞和历史承诺中释放。
- 周报显示计划额、执行额、未执行额、执行率、正式原因和未结转；工作台不再列为待处理。
- 将毛额记账与部分计划收尾纳入随版本安装的正式操作技能来源。

## v0.31.5 — 外部申购交易毛额成本

- 以外部申购实际支付毛额生成 BUY 交易，确认净额和手续费继续作为独立审计事实保存。
- 交易金额、持仓成本、派生现金支出与冻结计划执行额统一使用毛额，不重复计费。
- 对毛额、净额和费用执行整数货币精度核对，超过一最小货币单位的差异必须拒绝记账。
- 为既有未提交净额交易草稿提供范围受限的原地修订，保留原业务身份并要求新的明确确认。
- GitHub 发布完成后停止；真实草稿的读取、修订、确认记账与计划后续操作由 Hermes 负责。

## v0.31.4 — 确认时间精度与草稿修订

- 用 `EXACT` 和 `DATE_ONLY` 区分真实确认时刻与仅有确认日期的来源事实。
- 日期级确认以业务日期为权威，只保存兼容排序的内部归一时间，用户可见内容不显示零点。
- 为未提交的 PENDING/EXPIRED 确认草稿提供独立修订接口，保持原草稿、原申购和原幂等身份。
- 使用预期载荷摘要、事务条件更新、新确认凭据和修订审计防止并发覆盖或旧令牌复用。
- GitHub 发布完成后停止；四份真实草稿的部署后修订和确认记账由 Hermes 负责。

## v0.31.3 — 外部申购草稿安全重签

- 为已过期、从未提交且业务载荷未变化的外部申购草稿提供显式重签接口。
- 保留原草稿 ID、幂等键、载荷与载荷摘要，只轮换确认凭据和有效期并记录重签审计。
- 使用事务和条件更新保证并发重签最多只有一个成功结果。
- 查询过期草稿时确定性返回 `EXPIRED`；`SUBMIT` 与 `CONFIRM` 默认有效期调整为可配置的 24 小时。
- GitHub 发布完成后停止；真实草稿检查、重签、提交及生产升级由 Hermes 执行。

## v0.31.2 — 基金角色契约消歧

- 明确区分基金登记分类与当前组合策略角色，避免同名 `role` 表示两个业务概念。
- 保持当前策略角色为工作台、报告、持仓和周计划的唯一权威来源。
- 将旧的即时角色修改入口降级为兼容草稿入口，禁止绕过草稿、确认、提交治理。
- 提供数据库迁移、API/MCP 兼容说明和三类生产已观察场景的离线回归测试。
- GitHub 发布完成后停止；Windows/Hermes 部署与生产验收由 Hermes 执行。

## v0.31.1 — 最小业务闭环阻断修复

- 区分策略草稿中的字段缺省与显式清空，覆盖全部可空策略配置字段。
- 为已冻结周计划提供独立、短期有效的关闭草稿和再次确认流程。
- 统一计划列表、工作台和 readiness 对 FROZEN、ACTION_REQUIRED 与后续计划占用的口径。
- 不包含名称修正、新周计划、真实申购、卫星扩展、自动交易或研究功能。

## v0.31.0 — 场外申购确认生命周期

- 记录用户已在外部平台提交但尚未确认的申购事实。
- 支持在途、部分确认、全部确认、取消、拒绝和可审计更正。
- 分离提交日期、净值日期和份额确认日期，明确费用口径。
- 在途金额参与周计划防重复分配，但不进入持仓或正式成交金额。
- 最终 BUY 仍需用户单独确认，并复用现有交易账本与计划部分执行能力。
- 日报、周报、投资工作台和 readiness 读取新生命周期事实。
- 修正卫星直接定投关闭不应阻断核心舱最小闭环的 readiness 语义。

## v0.32.0 候选 — 报告投递闭环

- 独立实现日报/周报事实包生成、调度、outbox、提供商接受、用户接收验证和失败恢复。
- 不把按需报告内容生成冒充自动投递完成。

## 后续验收

- 在用户逐笔确认真实外部事实的前提下，完成首次周计划部分执行、全部执行、日报、周报和周期复盘。
- 达到连续运行门槛后进行 V1 发布候选审计。
- 卫星信号映射和扩展保持暂停，直到主业务闭环完成并由 Ryan 重新确定优先级。
