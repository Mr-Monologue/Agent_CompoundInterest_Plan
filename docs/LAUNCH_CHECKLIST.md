# 唯一上线执行清单与交接记录

当前已上线版本：v0.35.0（2026-09-23）。从文末最新交付节恢复；前文为对应版本的历史记录，不代表当前缺口。唯一需求/已有实现/缺口/验收/下一步均在本文件。
用户已授权本轮开发、测试、源码发布、备份和生产部署；投资业务提交仍需逐项确认。
不提交任何个人投资事实、账户标识或凭据到源码仓库。

## 已完成
- 基线核对：正式源码 v0.32.0，feature 工作区干净；长期分支不变。
- 识别默认上下文 GET 自动保存和周报草稿 GET 自动标记过期的只读副作用。
- P0 范围锁定，不重建账本、不修复 MCP、不迁移通知或开机未登录服务。

## 实现完成
- 日常 HTTP 客户端：查询、明确预算预览、受治理草稿与提交；写请求不自动重试。
- 纯读取默认上下文和周报草稿列表；保留现有兼容接口。
- 调度审计、数据日期与质量展示、医疗主题受限研究。

## 测试与发布已完成（2026-09-23）
- 17 项专项回归通过：只读不改原表、确认门禁、并发/重启防重、丢失响应、数据警告与缺失、医疗研究边界。
- 独立真实 HTTP Core 进程与客户端查询/预览通过；不代表生产已验收。
- Ruff、mypy（39 文件）、受管 PowerShell 解析、wheel/sdist 构建通过。
- 完整 Windows 回归首轮 303 通过、1 项旧握手超时；握手专项重测通过，最终全套复测 305 项通过，后续补充三项边界测试已随 17 项专项通过；CI 将复核最终 308 项。
- 最终 308 项测试在 Ubuntu 和 Windows CI 均通过；Ruff、mypy、PowerShell 解析通过。隔离真实 HTTP 测试与生产验收分别记录，未使用测试数据代替生产结果。
- PR #147、#148、#149 按 feature → develop → release → main 合并，每次合并前双平台 CI 通过。合并后 main CI 35812515378 亦通过。
- v0.33.0 GitHub Release 已正式发布；develop、release、main、Tag 与已审查代码的 Git tree 一致。
- 升级前数据库完整性检查通过，配置及任务定义备份可读；升级器另保留代码与数据库回滚快照。
- 本机 Core 从 v0.32.0 升级至 v0.33.0，健康和就绪通过；迁移检查仍为 0037_background_scheduler，本版无新增 schema。
- 安装的 192 个跟踪文件与 Tag 一致；环境、Codex、Hermes 配置及原后台调度任务定义未变。
- 已安装 investor-assistant 的 investment、plan、system、week、research 五项命令实际请求生产 Core 成功。
- 升级前后及五项演示后，11 项 HTTP 业务查询与 87 张原有表内容一致；对比排除备份元数据、迁移版本及持续心跳表，不排除投资事实。
- 本轮没有策略、财务事实、计划冻结或周报提交，没有行情手工同步、历史补跑或通知迁移。
- 详细真实业务输出保留在本机私有备份目录的 LIVE_ACCEPTANCE.md 与相邻原始响应；不把账户、持仓、内部标识或凭据提交到 Git。

## 当前交接
- 日常自然语言入口及确认边界见 CODEX_DAILY_OPERATIONS.md。HTTP 已可用，不等待 MCP。
- 三项任务继续由现有 Windows 后台来源执行，时区、政策及参数未变；账户为 S4U / Limited。Core 本身仍为登录后启动，不能宣称开机未登录可用。
- 当前心跳已恢复；升级窗口短暂连接失败保留为历史异常。执行成功不等于行情独立验证、完整风险覆盖或通知送达。
- 已有历史周报草稿按实际过期/已提交状态展示，保留误码、跳过原因及估值缺失；不自动续签或重复创建。
- 本文件的部署后交接补充仅提交 feature，未改变已发布版本；develop/release/main/Tag 保持发布树一致。
- 回滚沿用 runtime/windows/update-value-dca.ps1 的备份恢复流程及 docs/windows-background-scheduler.md；本次无故障回滚，不能把测试回滚称为本次生产回滚实测。

## 阻塞与边界
- 本轮 P0 无未解决的上线阻塞；下一阶段优先明确尚未配置的风险规则及数据验证要求，策略变更仍须草稿确认。
- 本周预览预算已由用户明确给出，仅预览且不结转历史未投入金额。
- 研究必须区分事实、推断、缺失；没有因果证据则不宣称板块上涨原因已确认。
- 真实休眠、未登录启动、通知送达与长期稳定性后续验收。

## v1.7 候选审计与 v0.33.1 实施（2026-09-23）

附件版本 architecture-v1.7.0-draft.2；原文保留，不自动覆盖生产。当前实际策略由 Core 返回 v1.6；软件版本不等于策略版本。

| 规则 | 当前生效/源码证据 | 候选要求 | 实现状态 | 策略确认 |
|---|---|---|---|---|
| 无合格卫星 | market_data._instrument_plan_items 已保留分给卫星但无资格的金额；全额核心也可能来自目标缺口公式 | 保留资金，仅批准备用顺序可改投 | 已有基础保留；未实现候选备用顺序 | 新备用顺序需要；本轮不改 |
| WARNING 金额 | v1.6 可生成基于 WARNING 的金额预览；不得宣称 v1.7 达标 | WARNING 只展示/影子，不改变金额或 OPEN | 冲突明确保留；本轮增加版本与限制提示 | 切换门槛需要，经 E 评审，不在本轮激活 |
| 卫星信号 | 现有同日同政策 OPEN 门禁，signals.py 已有质量检查；不是完整 v1.7 thesis/模型门禁 | 字段用途质量、版本论点、准入及晋级 | 部分实现 | 新准入规则需要 |
| 费用与执行 | v0.31.5+ 已按毛额进入成本/现金/计划，净额和费用独立审计，不重复加费 | 附件较早的确认金额描述 | 保留已上线毛额契约；不回退历史 | 改金额口径需另审，本轮不改 |
| 执行限额 | 策略最小/最大金额并非账户单日限额；无平台来源/有效期/交易日历存储 | 实例约束与可执行拆单 | 本轮明确 UNKNOWN、禁止假定天数；持久化约束与真实日历仍缺 | 写入适用约束/改变分配另按治理确认 |
| 运行形态 | Codex + Core HTTP + Windows 后台，Hermes 其他能力保留 | 原文 Hermes 形态 | 按用户最新方向；不重复迁移 | 普通实现无需策略确认 |

| 阶段/需求 | 已有实现 | 剩余缺口 | 验收证据 | 下一步 |
|---|---|---|---|---|
| A 订单/执行/费用 | subscriptions.py、planning.py 多笔/部分确认、在途、毛额、冲正/防重；weekly_reports.py 跳过和收尾 | 卖出外部订单不是通用 BUY/SELL 订单模型；真实完整闭环不可伪造 | 既有 subscriptions/planning/weekly_reports 测试；v0.33.0 现场只读验收 | 复用现有确认流程，不重建表 |
| A 限额/拆单 | 策略金额上下限 | 账户渠道来源/有效期、日历与确认规则缺失 | 新 decision_context 回归：金额不变、未知不生成日历 | 明确标记未知；后续受治理来源录入与日历 |
| A 报告/送达 | 周报草稿/正式报告与 outbox/回执基础 | 新通知渠道及真实日报/周报/月报接收未验收 | 不以生成报告或心跳代替送达 | 后续通知任务；本轮不发通知、不交周报 |
| B 最小研究解释 | 旧 thesis_status、研究证据、复盘动作；本轮 Core research-diagnosis | 完整版本论点、return_driver、MAPER 证据与持久化 decision_journal 未完成 | 新 HTTP 只读测试；状态/证据条数不升级为论点或买入信号 | 先展示事实、缺失和复核问题；逐只补证据 |
| C 环境影子 | 无四季注册/概率/回放引擎 | 数据、模型、稳定机制、样本外观察 | NOT_IMPLEMENTED，概率空 | 不声明已 SHADOW；后续隔离研究任务 |
| D 候选/相对表现 | 已有发现/观察清单/研究流水线 | v1.7 排位稳定、确认强基准相对归因、影子替换未完成 | 无足够基准/窗口返回 DATA_BLOCKED | 保持研究，禁止排名代替金额 |
| E 晋级评审 | 现有配置草稿确认可复用 | C/D 及样本外、成本、独立评审未完成 | 未激活，未通过 | 不创建 v1.7 激活草稿 |

当前实现：修正 HTTP 预览“可执行金额”误导为“候选分配金额”，保留原金额算法；执行约束未知时不生成拆单日历。新增 Core 研究诊断，客户端仅取回展示；不编造持有理由、收益来源、评分、概率或因果。完整 A/B 不宣称完成。
验证与发布（2026-09-23 完成）：
- Windows 隔离全套 312 项通过；21 项专项通过；Ruff、mypy（40 文件）、wheel/sdist 通过。
- PR #150 / #151 / #152 合并前均完成 Ubuntu + Windows CI；对应 35814938582 / 35815734380 / 35816320115。发布工作流 35816314506 成功。
- v0.33.1 已正式发布、本机升级并通过健康与就绪；develop/release/main/Tag 与已审查代码树一致，安装 196 文件与 Tag 一致。无新增 schema，仍为 0037。
- 已安装五项日常入口实际 HTTP 调用通过；11 项业务事实与升级前一致，候选金额算法和具体预览不变；执行约束未知、模型/论点缺失不被隐藏。
- 数据库备份完整性通过；原环境/Codex/Hermes 配置以及后台调度和 Hermes 任务定义均未变。无计划冻结、财务事实或周报提交。
- 首次升级从开发目录调用，在备份阶段因相对数据库路径找不到而中止（安装前）。已恢复原服务并验证健康，改从安装目录调用后成功升级。旧升级器依赖调用目录的加固仍是运维欠项；不得声称代码已修复。
- 后续升级必须先校验当前目录等于 InstallDir，再运行既有更新器；不得把部分失败备份目录当作完整回滚集。可用回滚集与实际日志路径保存在私有验收记录。
- 本轮部署后补记仅在 feature；已发布 develop/release/main/Tag 保持一致。实例约定和生产快照仅保存在 Git 排除文件；真实业务与研究附件保存在私有验收目录。

下一步优先：完成带渠道/份额/来源/有效期的受治理限额存储和交易日历，再验证可执行拆单。旧升级器的调用目录与安装前失败恢复需加固。B 的版本化论点、收益来源和日志，C/D 影子模型及 E 激活仍未完成；不扩大本轮上线结论。


## v0.34.0 执行证据交付单元（已上线，2026-09-23）

- 需求：可复用证据链、受确认的账户渠道约束、精确日历与分段拆单、升级器目录/恢复修复。
- 已实现：复用 research evidence/change history；新增两张约束治理表（0038）；HTTP/日常客户端贯通；候选资金守恒，已提交申购占额，来源冲突/未知不排单。
- 已实现：升级预检先于停服、安装目录解析、完整备份标记/指纹、部分失败不恢复、原服务恢复后健康验证。
- 验收进行中：隔离计算与治理测试、真实 PowerShell 任意目录和备份失败路径已通过；Windows 全套 326 项、Ruff、mypy（41 文件）、PowerShell 解析及 wheel/sdist 构建通过；独立真实 HTTP 证据归档/幂等/再次查询和只读不变性通过。双平台 CI、发布、部署及真实 200 元验收待本节补记。
- 公开来源：本轮重新取得华安限额 XLS/产品页、QDII 年历、上交所中秋/国庆安排、旧医疗年报原件；新版限额转载保持 REPOST，不提升成账户已验证约束。原件与指纹先私有保存，部署后经 Core 归档。
- 外部依赖：账户具体适用限额/剩余额度/渠道截止仍需账户页面，不妨碍通用计算和发布；不以公开来源归档替代配置确认。
- B–E、通知送达、开机未登录、真实休眠/重启与长期稳定性仍按上表保留。
- 详细操作及恢复契约见 [EXECUTION_EVIDENCE](EXECUTION_EVIDENCE.md)。

- 发布前完整性复核补充：账户当日剩余额度使用独立带时间/有效期的证据快照，扣除观察后新申购；未来安排须执行前再核额度。缺失/过期/冲突不冒充可执行。统一发布仍在进行，未部署中间版本。

- PR #153 最终 Ubuntu/Windows CI 均通过；#154 暂停提升以统一纳入账户余额度。该轮 Windows 隔离 PowerShell 预检在 45 秒进程上限超时，未发生业务断言失败；保留全部断言，将独立进程等待上限调整为 120 秒，后续最终双平台 CI 必须重新通过。


### 最终发布与生产验收（2026-09-23 16:00 上海时间）

- 已完成统一 v0.34.0 发布和本机部署，0038_execution_constraints 迁移完成，健康/就绪 PASS；当前策略仍为 v1.6。
- 最终隔离 CI：Windows 332 通过；Ubuntu 329 通过、3 项 Windows 专属测试跳过。Ruff、mypy、PowerShell 解析通过。覆盖渠道差异、周期内变化、已提交占额、休市/暂停/截止、冲突/未知、当日剩余额度、账户截图指纹与适用范围、任意目录升级及备份失败恢复。
- PR #153/#155 → #154 → #156 按长期分支顺序完成。最终双平台 CI 35832203763、35832964509、35833658623 通过；发布工作流 35833648719 通过。develop/release/main/v0.34.0 与最终审查代码树一致。
- 实际从开发目录调用新升级器完成升级，未使用正确 cwd 绕过。首次 GitHub TLS 中断发生在下载/停服之前；连接复检成功后重试，保留失败记录。完整代码/配置/任务/数据库回滚集通过标记、指纹和数据库完整性检查。未实际执行生产回滚，不把隔离故障测试称为生产回滚实测。
- 202 个安装文件与 Tag 内容一致；四项配置及 Windows 后台任务定义不变，后台来源/代次/历史三次漏跑不变；S4U / Limited 继续运行，升级后心跳已恢复，原下次执行时间保留。
- 五项已安装日常命令通过真实 Core HTTP 调用；升级前后、证据归档后，11 项业务事实与原计划分配对象一致。查询、预览未修改策略、账本、计划或周报。
- 13 条公开证据完成 Core 归档和再次读取，包含原文引用、来源、取得/发布/数据时间及指纹。医疗研究再次查询可引用已归档年报边界；转载及冲突保持其原质量，不提升为官方或账户适用证据。
- 生产账户约束未批准，预算预览的未知金额保留原基金归属；没有凭空生成日期、额度或改投资金。详细真实金额和原始响应仅保存在本机私有验收目录，不提交公共仓库。
- 剩余外部依赖：支付宝四只候选基金的买入页/交易规则（基金代码和份额、限额、当日剩余额度、截止及确认安排、规则生效时间；页面未提供的项目继续标未知）。收到证据后先归档、创建具体约束草稿，经用户确认才影响正式计划，不改 v1.6 分配规则。
- 本轮未冻结计划、未登记交易、未提交历史周报、未补跑历史漏跑。WARNING 与过期草稿照实保留；零投入不等于零收益。
- 后续 B 版本化论点/收益来源/日志、C/D 影子研究、E 激活评审、新通知、开机未登录与真实休眠/重启/长期运行验收仍未完成。
- 本节部署后交接仅提交 feature；已发布分支和 Tag 保持发布树一致。后续任务从本节及 EXECUTION_EVIDENCE.md 恢复；前文“进行中/待部署/缺少持久化”的内容为阶段历史。

## v0.35.0 阶段 B 交付（已上线，2026-09-23）
- 账户取证独立待办：优先 040046，不再要求四只齐备。其他买入页先尝试读取，仅对影响执行的缺失项集中补问；公开资料自行收集。稳定限额、截止规则和动态额度分别存储。
- 实现：只追加版本化研究草稿、收益来源画像、MAPER 事实/假设/反证、绑定版本的决策日志；研究未确认，不改变投资资格、策略或资金。
- 风险：复用真实策略字段和生命周期观察，分类配置、数据、基准、模型与用户目标缺口；提案不填默认阈值。
- 案例：优先 003096，复用已有证据并核对最新中报、产品概要、招募说明书及基准变更公告；原始买入理由未知，不能代写。
- 验收、双平台 CI、发布、部署及真实研究草稿归档已完成，证据见下。恢复入口 docs/RESEARCH_NOTEBOOK.md。
- B 论点激活需具体内容确认；C/D/E、通知及运行稳定性事项继续保留，不作为本版已完成能力。

### 最终发布与生产验收（2026-09-23 19:52 上海时间）

- Windows CI 337 项通过；Ubuntu 334 项通过、3 项 Windows 专属跳过。Ruff、mypy（42 文件）、PowerShell 解析及构建通过。覆盖并发版本/幂等、日志追加、跨基金引用拒绝、动态额度金额及范围匹配、只读不变性；未删减原执行约束和升级恢复验收。
- PR #157/#159/#160 → #158 → #161 按长期分支完成；最终双平台 CI 为 35853877910、35854617999、35855517298；发布工作流 35855507894 成功。develop/release/main/v0.35.0 与最终审查代码树一致。
- 从开发目录调用升级器，完成 v0.34.0 → v0.35.0 和 0039_research_notebook 迁移；95 表、健康/就绪 PASS。升级前数据库、配置及任务备份可读；完整回滚快照标记、指纹和数据库校验通过。本次没有生产故障回滚。
- 206 个安装文件与发布 Tag 一致。四项原配置、后台任务定义、来源/代次与三次历史漏跑不变；S4U / Limited，最近运行返回 0，升级后心跳恢复。停服窗口连接失败保留为历史事实，未手动补跑或同步行情。
- 七项已安装入口实际调用 Core HTTP：investment、plan、week、system、research、case、risk-coverage。升级前后及研究归档后，11 项业务查询与原预览分配对象一致；现行策略、持仓、交易、现金、计划和周报不变。
- 新增公开来源已通过 Core 归档并再次读取；复用原证据，保存原件/引用、发布日期、数据日期、获取时间及指纹。公开新公告能更新公开规则判断，不能自动批准账户适用性；旧转载记录和历史限制保留。
- 003096 已形成版本化研究草稿、完整 MAPER 栏目、收益来源研究画像、反证和决策日志。当前假设、管理人披露、未知原始买入理由分别保存；仍缺独立归因和用户原始意图，未升格为可改变资金的已批准论点。其余持仓只有初始研究档案，未宣称深研完成。
- 风险覆盖实际读取配置、估值及观测，形成先核对基准与数据口径、再讨论期限和阈值的提案。未填阈值、未改基准或策略，未执行风险扫描或产生卖出动作。
- 动态额度与稳定限额/截止规则独立保存；截图只证明观察时点。账户取证优先 040046：目前没有可访问的登录买入页，需其买入/交易规则页面的份额、渠道限额、当日可用额度与截止信息；页面没有的项目继续 UNKNOWN。其他基金先读取可访问页，再补实际阻断字段；逐只形成草稿，不等四只齐备。公开日历和公告继续自行获取。
- 实际账户、持仓金额、研究原始响应、来源附件与恢复路径仅保存在本机私有验收目录和 Git 排除生产快照；不提交公共仓库。详细入口见 RESEARCH_NOTEBOOK.md。
- 尚未完成：其余持仓深研、原始意图收集、论点具体确认及激活治理、独立数据/归因，C/D 影子模型与 E 晋级、新通知、开机未登录、真实休眠/重启/长期运行验收。保留 WARNING、过期周报及历史误码，不以零投入推断零收益。
- 该部署后交接仅提交 feature，不改已发布分支/Tag；后续优先沿现有研究档案补证，不从零重复研究，也不重复以同一预算候选分配作为交付。

## v0.36.0 基准与相对表现诊断（2026-09-24，进行中）

- 已有能力：v0.35.0 证据、论点、MAPER 和决策日志；复用，不重建。
- 新实现：0040 两张独立研究表、复合与历史区间、具体确认治理、逐日收益/分红、披露核对及只读客户端。官方基准与诊断建议分开，不提升为 STRONG，不进入正式风险动作。
- 真实研究：已取得管理人净值、完整分红列表、两条官方指数同区间历史和变更公告，隔离重算含跨变更窗口；长期重算差异保持 WARNING。私有候选/来源表不提交账户事实。
- 验收：Windows 全套 348 项及最终新增差异测试 12 项专项通过；最终静态、双平台 CI、发布和部署待下方补记。
- 下一步：连续完成发布、可恢复备份、本机升级和实际 HTTP 归档/查询；仅研究映射具体批准留给用户。
- 未完成：其他基金深研、原始买入意图、独立归因/数据、风险阈值确认、账户证据、通知及 C/D/E、开机未登录和真实休眠/长期运行。策略 v1.6 保持有效。
