# 唯一上线执行清单与交接记录

目标版本：v0.33.0。范围：复用 Core 的日常 HTTP 入口及只读机会解释。
用户已授权本轮开发、测试、源码发布、备份和生产部署；投资业务提交仍需逐项确认。
不提交任何个人投资事实、账户标识或凭据到源码仓库。

## 已完成
- 基线核对：正式源码 v0.32.0，feature 工作区干净；长期分支不变。
- 识别默认上下文 GET 自动保存和周报草稿 GET 自动标记过期的只读副作用。
- P0 范围锁定，不重建账本、不修复 MCP、不迁移通知或开机未登录服务。

## 实现完成
- 日常 HTTP 客户端：查询、明确预算预览、受治理草稿与提交；写请求不自动重试。
- 纯读取默认上下文和周报草稿列表；保留现有兼容接口。
- 调度审计、数据日期与质量展示、医疗主题受限研究。

## 测试与发布已完成（2026-09-23）
- 17 项专项回归通过：只读不改原表、确认门禁、并发/重启防重、丢失响应、数据警告与缺失、医疗研究边界。
- 独立真实 HTTP Core 进程与客户端查询/预览通过；不代表生产已验收。
- Ruff、mypy（39 文件）、受管 PowerShell 解析、wheel/sdist 构建通过。
- 完整 Windows 回归首轮 303 通过、1 项旧握手超时；握手专项重测通过，最终全套复测 305 项通过，后续补充三项边界测试已随 17 项专项通过；CI 将复核最终 308 项。
- 最终 308 项测试在 Ubuntu 和 Windows CI 均通过；Ruff、mypy、PowerShell 解析通过。隔离真实 HTTP 测试与生产验收分别记录，未使用测试数据代替生产结果。
- PR #147、#148、#149 按 feature → develop → release → main 合并，每次合并前双平台 CI 通过。合并后 main CI 35812515378 亦通过。
- v0.33.0 GitHub Release 已正式发布；develop、release、main、Tag 与已审查代码的 Git tree 一致。
- 升级前数据库完整性检查通过，配置及任务定义备份可读；升级器另保留代码与数据库回滚快照。
- 本机 Core 从 v0.32.0 升级至 v0.33.0，健康和就绪通过；迁移检查仍为 0037_background_scheduler，本版无新增 schema。
- 安装的 192 个跟踪文件与 Tag 一致；环境、Codex、Hermes 配置及原后台调度任务定义未变。
- 已安装 investor-assistant 的 investment、plan、system、week、research 五项命令实际请求生产 Core 成功。
- 升级前后及五项演示后，11 项 HTTP 业务查询与 87 张原有表内容一致；对比排除备份元数据、迁移版本及持续心跳表，不排除投资事实。
- 本轮没有策略、财务事实、计划冻结或周报提交，没有行情手工同步、历史补跑或通知迁移。
- 详细真实业务输出保留在本机私有备份目录的 LIVE_ACCEPTANCE.md 与相邻原始响应；不把账户、持仓、内部标识或凭据提交到 Git。

## 当前交接
- 日常自然语言入口及确认边界见 CODEX_DAILY_OPERATIONS.md。HTTP 已可用，不等待 MCP。
- 三项任务继续由现有 Windows 后台来源执行，时区、政策及参数未变；账户为 S4U / Limited。Core 本身仍为登录后启动，不能宣称开机未登录可用。
- 当前心跳已恢复；升级窗口短暂连接失败保留为历史异常。执行成功不等于行情独立验证、完整风险覆盖或通知送达。
- 已有历史周报草稿按实际过期/已提交状态展示，保留误码、跳过原因及估值缺失；不自动续签或重复创建。
- 本文件的部署后交接补充仅提交 feature，未改变已发布版本；develop/release/main/Tag 保持发布树一致。
- 回滚沿用 runtime/windows/update-value-dca.ps1 的备份恢复流程及 docs/windows-background-scheduler.md；本次无故障回滚，不能把测试回滚称为本次生产回滚实测。

## 阻塞与边界
- 本轮 P0 无未解决的上线阻塞；下一阶段优先明确尚未配置的风险规则及数据验证要求，策略变更仍须草稿确认。
- 本周预览预算已由用户明确给出，仅预览且不结转历史未投入金额。
- 研究必须区分事实、推断、缺失；没有因果证据则不宣称板块上涨原因已确认。
- 真实休眠、未登录启动、通知送达与长期稳定性后续验收。
