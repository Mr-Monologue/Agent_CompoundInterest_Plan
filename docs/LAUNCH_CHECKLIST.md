# 唯一上线执行清单与交接记录

目标版本：v0.33.0。范围：复用 Core 的日常 HTTP 入口及只读机会解释。
用户已授权本轮开发、测试、源码发布、备份和生产部署；投资业务提交仍需逐项确认。
不提交任何个人投资事实、账户标识或凭据到源码仓库。

## 已完成
- 基线核对：正式源码 v0.32.0，feature 工作区干净；长期分支不变。
- 识别默认上下文 GET 自动保存和周报草稿 GET 自动标记过期的只读副作用。
- P0 范围锁定，不重建账本、不修复 MCP、不迁移通知或开机未登录服务。

## 实现完成
- 日常 HTTP 客户端：查询、明确预算预览、受治理草稿与提交；写请求不自动重试。
- 纯读取默认上下文和周报草稿列表；保留现有兼容接口。
- 调度审计、数据日期与质量展示、医疗主题受限研究。

## 正在做：测试与发布
- 16 项专项回归通过：只读不改原表、确认门禁、并发/重启防重、丢失响应、数据警告与缺失、医疗研究边界。
- 独立真实 HTTP Core 进程与客户端查询/预览通过；不代表生产已验收。
- Ruff、mypy（39 文件）、受管 PowerShell 解析、wheel/sdist 构建通过。
- 完整 Windows 回归首轮 303 通过、1 项旧握手超时；握手专项重测通过，最终全套复测 305 项通过，后续补充两项边界测试已随 16 项专项通过；CI 将复核最终 307 项。
- Windows 本地全套及 Ubuntu/Windows CI；feature → develop → release → main。
- 可读备份、部署、安装源码比对、健康/就绪与真实查询验收。
- 实际演示与最终交接；详细生产证据仅存本机私有备份目录。

## 阻塞与边界
- 本周预览预算已由用户明确给出，仅预览且不结转历史未投入金额。
- 研究必须区分事实、推断、缺失；没有因果证据则不宣称板块上涨原因已确认。
- 真实休眠、未登录启动、通知送达与长期稳定性后续验收。
