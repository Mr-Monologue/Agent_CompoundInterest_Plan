{
  "enabled": false,
  "name": "automation-retry-due",
  "schedule": "*/5 * * * *",
  "timezone": "Asia/Shanghai",
  "no_agent": true,
  "script": "value_dca_retry_due.py",
  "note": "Retries only persisted failed runs whose backoff elapsed; no due retry produces empty stdout."
}
