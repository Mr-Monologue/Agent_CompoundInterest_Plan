# 大阶段一：公共策略实例与完整周计划

状态：实现完成，待发布  
目标版本：0.11.0

## 阶段目标

把 Value DCA 从“带有单个用户配置痕迹的本地投资助手”升级为可供不同用户复用的 Agent：

- 公共策略只定义通用规则，不携带真实基金代码、账户或持仓。
- 每位用户在本地拥有独立的策略实例和标的白名单。
- Core 生成可审计、可确认、可追踪的逐标的周计划。
- 计划、真实成交和交易账本保持严格分离。

## 已完成

- 新增公共策略定义、不可变策略版本和显式组合策略绑定。
- 新增组合级标的配置：角色、定投资格、目标权重、优先级、金额边界、基准和投资论点。
- 移除公共 API、MCP 和普通标的登记中的全局角色/定投资格配置入口。
- 旧配置迁移为本地策略实例；历史角色得到保留，但定投资格默认关闭。
- 新组合不再自动获得策略、角色或具体定投标的。
- 周计划只从当前策略实例的显式定投白名单选择标的。
- 支持确定性权重拆分、最小/最大金额、金额保留和无合格标的拦截。
- 新增计划持久化与版本化：`DRAFT`、`FROZEN`、`EXECUTED`、`EXPIRED`、`SKIPPED`。
- 冻结计划需要一次性确认令牌；标记执行必须引用独立、已提交的真实 BUY 交易。
- REST、MCP、CLI、Hermes Skill、安全规则和输出模板已同步。
- Windows 安装器沿用既有自动迁移、备份、回滚和 Skill 同步机制。

## 明确不做

- 不在公共仓库预设任何真实基金为主要定投标的。
- 不因用户持有、登记或曾经购买某基金而自动开放定投资格。
- 不让模型在 Core 白名单之外选择基金或自行计算替代金额。
- 不连接券商或支付宝执行交易。
- 不把计划冻结、用户口头同意或 Agent 回复当作成交。
- 本阶段不实现 PE 分位、完整风险引擎、卖出引擎和自动 Cron 投决。

## 下一大阶段

大阶段二聚焦确定性投决能力：估值区间与 PE 分位、数据质量门、风险规则、卖出双引擎和
可审计建议书。它会消费本阶段形成的公共策略版本和用户策略实例，但不会改变本阶段的
安全边界。
