# 大阶段三状态

日期：2026-07-27
目标版本：0.15.0

## 本阶段已实现

- 新增组合本地自动化策略草稿、确认提交、版本化启停和审计记录。
- 所有公共自动化默认未配置；没有用户确认的策略时任务只记录 `SKIPPED` 并静默。
- 首批确定性任务覆盖行情同步、风险扫描、周计划草稿、卖出后到期复盘和系统自检。
- 周计划自动化必须保存用户明确批准的固定新增资金，只生成 `DRAFT`，不冻结、不成交。
- 每次运行使用任务、组合、计划时间和策略版本组成稳定幂等键。
- 运行保存心跳、尝试次数、最大次数、下一次重试时间、错误码和结构化输出。
- 新增 5/15/30 分钟退避状态和 `retry-due` 确定性重试入口。
- 新增不可变报告事实包，确定性标记 `SILENT` 或 `NOTIFY`。
- 新增系统告警和通知 outbox；`NOTIFY` 只表示等待投递，不伪称已经送达。
- 新增自动化状态、运行历史、报告事实包和告警的只读 MCP 工具。
- Hermes Agent Cron 只读取事实包，不能配置策略、执行任务或调用任何确认写工具。
- 公共仓库不包含用户基金、账户、资金金额、投递对象或启用状态。
- Core 可从活动策略生成精确的 Hermes 受管任务清单，任务名统一使用 `value-dca-` 前缀。
- 新增 Hermes 调度快照与确定性漂移判断，区分未对账、定义漂移、Gateway 阻塞和完全一致。
- 安装器把无窗口 Python 运行桥复制到当前 Profile 的 `scripts` 目录，但不擅自启用任务。
- 运行桥直接调用本机 Core API，不依赖项目绝对路径、`uv` 环境锁或 shell 日期占位符。
- 计划日期缺省时由 Core 按已批准策略时区确定，稳定幂等键仍由 Core 负责。
- 到期重试作为独立受管任务进入调度清单；无到期重试时保持空输出。
- Agent 对账只能管理清单前缀内的任务；重复名称、时区冲突或 Gateway 停止必须中止。

## 保持不变的安全边界

- 自动风险扫描只读取当前组合已批准规则；命中最多生成待复核卖出建议。
- 自动周计划只创建未冻结草稿。
- 自动卖出、计划冻结、建议批准、交易提交和策略修改均不存在无人值守路径。
- 主机重启后可以重试确定性任务，但不会重放用户确认动作。

## 下一阶段

- 在真实 Hermes Profile 完成首次清单对账并观察至少一个调度周期。
- 实现通知 outbox 与 Hermes 渠道结果之间的可验证投递回执、失败重试和去重闭环；在
  Hermes 没有提供渠道证据前只允许记录“交给调度器”，不能标为“已送达”。
- 增加官方净值回填、数据质量检查和来源化研究事实采集。
- 在连续运行验证后，再提供显式选择的一键对账向导。
