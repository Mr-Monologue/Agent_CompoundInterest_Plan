# 四大阶段完成度审计

审计日期：2026-07-29  
代码基线：0.16.0

本文件按可运行代码、数据库迁移、API/MCP 合约和自动化脚本核对，不把设计文档中的目标
当作已经交付的功能。

## 结论

| 大阶段 | 状态 | 已形成闭环 | 仍未闭环 |
|---|---|---|---|
| 一：公共策略与完整周计划 | 基本完成 | 公共策略定义/不可变版本/用户实例分离；显式绑定；实例标的范围；角色与投入资格分离；估值证据、分位和质量闸门；逐标的 DCA；计划草稿、修订、冻结、跳过和预算保留 | 尚未内置真正独立于 Eastmoney lineage 的公共数据适配器；官方回填以来源化录入和验证基础为主，未形成全自动通用回填 |
| 二：投资生命周期与组合治理 | 部分完成 | 投资论点、代理约束、来源化生命周期观察；8 类确定性触发；卖出建议、诊断、人工决定、外部成交分离；过渡评估；卖出后六个月跟踪 | 通用观察池/候选基金排名、月/季/年持仓重检和独立再平衡建议尚未形成完整状态机 |
| 三：绩效、复盘与自动化运行 | 部分完成 | 受治理自动化策略；行情同步、风险扫描、周计划草稿、卖出复盘、系统自检；重试、漏跑补跑、事实包、告警、outbox、Hermes Cron 对账和 `[SILENT]` | Modified Dietz/XIRR/TWR、组合基准与归因、月/季/年复盘、参数效果评价、行动项质量、渠道投递回执、LLM L0–L3 降级尚未完成 |
| 四：V1 稳定化与公共分发 | 部分完成 | 公共仓库无个人持仓默认；GitHub Release 自动升级；一条 PowerShell 引导安装；升级前备份、迁移、失败回滚；Hermes Profile/Skill/MCP 安装 | 通用交互式初始化向导、完整恢复命令、策略模板选择体验、14 天连续运行验收、安全隐私与运维文档收口尚未完成 |

因此，不能宣称四个大阶段全部完成。当前产品已经覆盖阶段一的主要投资主链路、阶段二的
卖出治理主链路，以及阶段三的自动化运行底座；距离公共 V1 仍需要阶段三剩余能力和阶段四
产品化验收。

## 0.16.0 补跑机制

- 以活动自动化策略的五字段 Cron、策略时区、批准时间和策略版本为唯一事实来源。
- 计划时间过去 10 分钟仍没有 durable run 才标记为 `DUE`，避免与正常 Cron 竞争。
- 每项策略只恢复最近一次计划窗口，默认最多回看 7 天。
- 补跑使用原计划时间，正常运行、补跑和失败重试共享同一幂等身份。
- 兼容旧版本按本地业务日期保存的运行，升级后不会把历史成功误判成漏跑。
- 复用既有 `value-dca-retry-due` 五分钟脚本：先补漏，再重试，现有用户无需另装任务。
- 恢复动作只运行已批准的确定性作业，`automatic_trade` 始终为 `false`。

## 本地运行边界

| 主机状态 | 到点能否运行 | 恢复行为 |
|---|---|---|
| 锁屏，但电脑唤醒且用户未注销 | 能 | 正常 Cron 运行 |
| 睡眠或休眠 | 不能 | 唤醒后下一次五分钟恢复轮询补最近窗口 |
| 关机 | 不能 | 开机登录、Gateway 启动后补最近窗口 |
| 用户注销 | 通常不能 | 重新登录、Gateway 启动后补最近窗口 |
| 断网但进程运行 | 作业可能失败 | 进入已有退避重试；恢复网络后由同一恢复任务重试 |

本地 Agent 无法在物理关机时执行。若未来要求“电脑关闭仍准时运行”，需要单独部署常在线
主机或云端调度节点；这属于部署形态变化，不应由本地补跑伪装成实时执行。
